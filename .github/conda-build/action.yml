name: "enviroment-build"
description: "Actions to build conda and install pip"
runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v2
    - run: |
        conda env create -f environment.yml
        source activate cobramod
        pip install . -v
      shell: bash
